{
  "name": "support-pull-bot",
  "version": "1.0.0",
  "description": "Discord bot that auto-pulls users from a waiting room to an available admin, controlled via a single /support menu",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "deploy": "node deploy-commands.js"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  }
}
